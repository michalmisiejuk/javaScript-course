// 027 Statements/declaraions and Expressions

3+4         // expresion and doeas produces a value
1991        // also expresion and also will produce a value
true && false && !false

if (23>10) {                //statment that donsnt produce any value
    const str = '23 is bigger'; // declares a variable  called str but no produce any value string is an expression but its not a value
}

// Java script expect statment and expressios in different places f.e.
// in template literal u can only place expresions  not  statment cant place if else in `blabla {expresion} blabla`